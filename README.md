# qa-cuda-variant-test-feedstock

This is a dummy Conda feedstock used to test building a package with CUDA variants (e.g., v12 and v13).

The package installs a simple Python module that prints:

`hello world`

Build
=====

`conda build recipe`

Purpose
=======

This feedstock is intended for QA testing of Conda builds with CUDA variant support.